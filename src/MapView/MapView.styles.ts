import { styled } from "@mui/material";

export const FloatingMapToolbarContainer = styled("div")({
  position: "absolute",
  top: "15px",
  right: "15px",
  zIndex: 1,
});

export const MapViewContainer = styled("div")({});
